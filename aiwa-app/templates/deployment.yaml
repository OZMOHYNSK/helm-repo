apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "aiwa-app.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "aiwa-app.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "aiwa-app.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "aiwa-app.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}